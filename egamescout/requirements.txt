# ============================================================
# E-GameScout — Requirements
# Python 3.10+  |  XAMPP MySQL (PyMySQL driver)
# Install with: pip install -r requirements.txt
# ============================================================

# Core Framework
Django==6.0.1

# MySQL Driver (works with XAMPP out-of-the-box)
PyMySQL==1.1.0

# Environment variables (.env support)
python-dotenv==1.0.0

# Static file serving (replaces WhiteNoise on production too)
whitenoise==6.6.0

# Image processing (profile photos, scorecard uploads)
Pillow==11.3.0

# HTTP requests (used in helpers.py for Groq vision API calls)
requests==2.32.5

# AI — Google Gemini (Scorecard AI feature)
google-generativeai==0.8.6
google-genai==0.3.0

# AI — Groq fallback (Scorecard AI fallback)
groq==1.0.0

# User agent parsing (for device detection in auth)
user-agents==2.2.0

# Two-Factor Authentication
pyotp==2.9.0

# QR Code Generation
qrcode==8.0

# PDF Generation
xhtml2pdf==0.2.16


